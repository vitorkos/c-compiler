#include <stdio.h>

int main() {
    int n = 0;

    switch (n) {
        case 0:
            printf("n = 0");
            break;
        default:
            printf("não é 0");
    }

    return 0;
}